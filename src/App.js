import React from "react";
import ReactDom from "react-dom/client";

const AppLayout = () => {
  return <>This is my Food App - Foodie App</>;
};

const root = ReactDom.createRoot(document.getElementById("root"));
root.render(<AppLayout />);
